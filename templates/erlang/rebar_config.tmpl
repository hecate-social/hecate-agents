{erl_opts, [debug_info]}.

{deps, [
    {reckon_evoq, {git, "https://github.com/reckon-db-org/reckon_evoq.git", {branch, "main"}}}
]}.

%% Include spoke directories
{src_dirs, [
    "src"
    %% ADD SPOKE DIRECTORIES HERE
    %% "src/{{spoke1}}",
    %% "src/{{spoke2}}",
    %% "src/get_{{aggregate}}_by_id",
    %% "src/get_{{aggregates}}_page"
]}.
